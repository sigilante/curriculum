#hoon 

- Enumerate and distinguish use cases for dry cores (using variance):
  - Covariant (`%zinc`)
  - Contravariant (`%iron`)
  - Bivariant (`%lead`)
  - Invariant (`%gold`)

Runes introduced:  `^|`, `^&`, `^?`, `|~`, `|?`
Irregular syntax introduced:
